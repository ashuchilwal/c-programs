#include<stdio.h>
int main()
{
    float length = 20, width  = 15, area, costper = 400, total_cost;

    printf("area of ractangle = %f\n", area = length * width );

    printf("total cost is = %f\n", total_cost = costper * area);
    return 0;
}